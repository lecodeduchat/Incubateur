import React from 'react';
import Header from '../components/Header';

const Products = () => {
    return (
        <div>
            <Header />
            <h1>Produits</h1>
        </div>
    );
};

export default Products;